module.exports = {
  plugins: {
    // Tailwind's PostCSS plugin is now published as a separate package.
    // Install `@tailwindcss/postcss` and keep this key in sync with the package name.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}
